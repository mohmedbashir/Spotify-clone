class AppVectors {
  static const basPath = "assets/vectors/";
  static const format = ".svg";

  static const logo = "${basPath}logo$format";
  static const darkMode = "${basPath}moon$format";
  static const lightMode = "${basPath}sun$format";
}
